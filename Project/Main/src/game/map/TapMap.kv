<Map>:        
	mCat: the_cat
	mLitterBox: the_litter
	
    canvas:
        Color:
            rgb: 0.2, 0.8, 0.5
        Rectangle:
            size: self.width, self.height
	
	CatWidget:
		id: the_cat
		
	LitterBoxWidget:
		id: the_litter

	# Bars
	Label:
		text: "Energy: " + str(the_cat.mEnergy)
		pos: 10, root.top - 70
	Label:
		text: "Hapiness: " + str(the_cat.mHapiness)
		pos: 10, root.top - 85
	Label:
		text: "Litterbox: " + str(the_cat.mLitterBox)
		pos: 10, root.top - 100
	Label:
		text: "Litterbox: " + str(the_cat.mHygine)
		pos: 10, root.top - 115