<FoodBowlWidget>:
    Image:
    	source: "CatBowl.png"
    	pos: root.pos
		#size: root.size
        #keep_ratio: True
        allow_stretch: True
        size: root.size
        #width: self.texture_size[0] / app.root.width
        #height: self.texture_size[1] / app.root.height
        
    Label:
    	text: ("%d/50" % (root.mFoodValue))
		pos: (root.center_x, root.center_y + 10)